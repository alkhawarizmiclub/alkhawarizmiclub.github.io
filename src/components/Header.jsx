import Navbar from "./Navbar";

const Header = () => {
    return (
        <header className="h-screen w-full bg-blue-900">
            <Navbar />
        </header>
    );
}
 
export default Header;
